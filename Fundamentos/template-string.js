const nome = 'Felipe'
const template = `Olá meu nome é ${nome}`;
console.log(template);

//expressoes
console.log(`1 + 1 = ${1+1}`);

// funcoes
const up = texto => texto.toUpperCase()
console.log(`Ei... ${up('cuidado')}!`);

//via de regra vai usar mais com variáveis